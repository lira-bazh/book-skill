# План реализации skill для LiveLib wish-list

## Цель

Пользователь отправляет ссылку на обычную страницу списка книг LiveLib:

```text
https://www.livelib.ru/reader/LiraLantan/wish
```

Skill открывает публичную страницу LiveLib wish-list в браузерном режиме,
получает страницы wish-list с учетом пагинации и обновляет один JSON-файл
инкрементально. Если файл уже существует, skill обновляет его до актуального
состояния LiveLib: сохраняет данные книг, которые всё еще есть в wish-list,
добавляет новые книги и удаляет книги, которых больше нет на LiveLib. Затем
ищет на `https://books.yandex.ru/` только те книги, у которых еще нет ссылок
на Яндекс Книги.

Минимальные поля для каждой книги:

```json
{
  "title": "Сто лет одиночества",
  "authors": ["Габриэль Гарсиа Маркес"],
  "url": "https://www.livelib.ru/book/...",
  "yandex_books_urls": [
    "https://books.yandex.ru/books/..."
  ]
}
```

## MVP

Сделать один skill `livelib-wishlist`, который умеет:

1. [x] Принимать URL вида `https://www.livelib.ru/reader/<username>/wish`.
2. [x] Открывать страницу в браузерном режиме по умолчанию.
3. [x] Открывать публичную страницу wish-list без проверки авторизации.
4. [x] Использовать постоянный браузерный профиль для сохранения сессии.
5. [x] Находить ссылки пагинации и открывать все страницы wish-list.
6. [x] Находить книги в списке на каждой странице.
7. [x] Извлекать название, авторов и ссылку на книгу LiveLib.
8. [x] Сохранять результат в JSON-файл.
9. [x] Сообщать путь к файлу, количество найденных книг и количество обработанных страниц.

## Этап 2: ссылки на Яндекс Книги

После составления списка книг из LiveLib добавить обогащение через
`https://books.yandex.ru/`.

Skill должен уметь:

1. [x] Для каждой книги из результата LiveLib формировать поисковый запрос по названию и авторам.
2. [x] Открывать поиск Яндекс Книг в браузерном режиме.
3. [x] Находить ссылки на страницы книг на `books.yandex.ru`.
4. [x] Отбирать только результаты, похожие на исходную книгу по названию и авторам.
5. [x] Сохранять все подходящие ссылки в поле `yandex_books_urls`.
6. [x] Если книга не найдена на Яндекс Книгах, сохранять пустой массив `yandex_books_urls: []`.
7. [x] Ограничивать количество проверяемых результатов на книгу, например `--max-results 5`.
8. [x] Делать фиксированную задержку 2000 мс между запросами LiveLib и Яндекс Книг.
9. [x] Не перезаписывать LiveLib-поля при обогащении.
10. [x] Если выходной JSON уже существует, читать его и не искать заново книги, у которых `yandex_books_urls` уже заполнен.
11. [x] Сообщать количество книг, для которых найдены ссылки Яндекс Книг, и количество книг, пропущенных из-за уже существующих ссылок.

## Этап 3: инкрементальное обновление одного JSON

Новый основной порядок работы:

1. [x] В начале запуска проверять, существует ли выходной JSON-файл `--out`.
2. [x] Если файл существует, читать его до открытия LiveLib и считать его текущим состоянием библиотеки.
3. [x] При ошибке чтения или невалидной JSON-схеме останавливать запуск до любых сетевых действий.
4. [x] После парсинга LiveLib сопоставлять книги из сайта с существующими книгами по LiveLib `url`.
5. [x] Для уже существующих книг, которые всё еще есть на LiveLib, сохранять данные из файла, включая `yandex_books_urls` и любые дополнительные поля.
6. [x] Для новых книг из LiveLib добавлять объект с `title`, `authors`, `url` и `yandex_books_urls: []`.
7. [x] Удалять из выходного JSON книги, которых сейчас нет в LiveLib wish-list.
8. [x] Сохранять порядок LiveLib для всех актуальных книг после merge.
9. [x] После merge искать на Яндекс Книгах только книги, у которых `yandex_books_urls` отсутствует или пустой.
10. [x] При найденных ссылках обновлять только `yandex_books_urls`, не перезаписывая LiveLib-поля из файла.
11. [x] Записывать обновленный список обратно в тот же `--out` файл.
12. [x] Сообщать статистику: сколько книг было в файле, сколько найдено на LiveLib, сколько добавлено новых, сколько пропущено на Яндексе и сколько обогащено.

## Этап 4: ссылки на Литрес

После составления списка книг из LiveLib добавить обогащение через
`https://www.litres.ru/`. В отличие от Яндекс Книг, для этого сценария
потребуется авторизация пользователя в видимом браузере.

Skill должен уметь:

1. [x] Для каждой книги из результата LiveLib формировать поисковый запрос по названию и авторам.
2. [x] Открывать поиск Литрес в браузерном режиме с тем же постоянным профилем `--profile-dir`.
3. [x] Не автоматизировать логин: если пользователь не авторизован, открыть Литрес в видимом браузере, попросить войти вручную и продолжить после подтверждения.
4. [x] Хранить состояние авторизации только в локальном браузерном профиле, без отдельного сохранения cookie, паролей или токенов.
5. [x] Находить результаты поиска Литрес и извлекать название, авторов и URL страницы книги.
6. [x] Отбирать только результаты, похожие на исходную книгу по названию и авторам.
7. [x] Сохранять все подходящие ссылки в поле `litres_urls`.
8. [x] Если книга не найдена на Литрес, сохранять пустой массив `litres_urls: []`.
9. [x] Ограничивать количество подходящих результатов на книгу универсальным параметром `--max-results`, например со значением по умолчанию `5`.
10. [x] Делать фиксированную задержку 2000 мс между поисковыми запросами Литрес.
11. [x] После merge искать на Литрес только книги, у которых `litres_urls` отсутствует или пустой.
12. [x] Если выходной JSON уже существует, переносить заполненные `litres_urls` без повторного сетевого поиска.
13. [x] При найденных ссылках обновлять только `litres_urls`, не перезаписывая LiveLib-поля и уже сохраненные данные из файла.
14. [x] Сообщать количество книг, для которых найдены ссылки Литрес, количество пропущенных книг с существующими `litres_urls` и количество новых обогащений.

Технический план:

1. [x] Добавить модуль `scripts/lib/litres-books.mjs` по аналогии с `yandex-books.mjs`.
2. [x] Реализовать функции построения запроса, построения URL поиска, нормализации URL Литрес, извлечения результатов, сопоставления и обогащения.
3. [x] В `scripts/lib/browser.mjs` добавить браузерный fetcher для поиска Литрес.
4. [x] Для Литрес по возможности использовать один persistent browser context на всю серию поисков, чтобы не создавать гонки за профиль и не открывать браузер на каждую книгу.
5. [x] В `scripts/livelib-wish-to-json.mjs` убрать отдельные CLI-флаги `--with-yandex-books` и `--with-litres`; поиск ссылок на Яндекс Книгах и Литрес выполнять как часть основного browser workflow.
6. [x] Заменить отдельные лимиты результатов на универсальный CLI-флаг `--max-results <n>`, применяемый ко всем поисковым обогащениям.
7. [x] Обновить merge существующего JSON так, чтобы дополнительные поля, включая `yandex_books_urls` и `litres_urls`, сохранялись для актуальных книг.
8. [x] Обновить `SKILL.md`: описать ручную авторизацию на Литрес, локальное хранение сессии в профиле и универсальный CLI-флаг `--max-results`.
9. [x] Добавить тесты `tests/litres-books.test.mjs`, а также обновить CLI/browser-тесты моками для нового fetcher.

## Этап 5: рефакторинг браузерного workflow

Изменить браузерный workflow так, чтобы Playwright запускал Chromium только
один раз за выполнение CLI-команды, а все сетевые действия выполнялись в одном
persistent context и одном видимом окне.

Цель рефакторинга:

1. [x] Убрать последовательное открытие нескольких Chromium context для LiveLib, Яндекс Книг и Литрес.
2. [x] Создать единый browser session на весь основной workflow.
3. [x] Использовать один persistent profile `--profile-dir` и один видимый browser context для всех доменов.
4. [x] Переиспользовать одну страницу `page` для LiveLib-пагинации, поиска Яндекс Книг, ручного логина Литрес и поиска Литрес.
5. [x] Сохранить ручной логин Литрес: открыть Литрес в том же окне, дождаться подтверждения пользователя и продолжить в этой же сессии.
6. [x] Сохранить текущую инкрементальную логику: существующий JSON читается до открытия браузера, а сетевые запросы выполняются только для незаполненных полей.
7. [x] Сохранить фиксированные задержки 2000 мс между переходами, не добавляя параллельных запросов к одной странице.
8. [x] Гарантировать закрытие browser context в `finally` при успехе, ошибке парсинга, ошибке поиска или остановке из-за защитной страницы.
9. [x] Оставить unit-тесты быстрыми: тесты не должны открывать реальный браузер, только fake Playwright/session.
10. [x] Сохранить публичный CLI-контракт для `--profile-dir`, `--max-pages`, `--max-results`; браузерный режим сделать основным без отдельного флага.

Технический план:

1. [x] В `scripts/lib/browser.mjs` добавить session API, например `withBrowserSession({ profileDir, playwright }, callback)`.
2. [x] Внутри session API один раз вызвать `chromium.launchPersistentContext(resolveProfileDir(profileDir), { headless: false })`.
3. [x] В session API создать или взять первую страницу `context.pages()[0] ?? await context.newPage()`.
4. [x] Вернуть в callback набор методов поверх этой страницы: `fetchWishlistPages`, `fetchYandexBooksSearchPage`, `fetchLitresSearchPage`, при необходимости `openLitresHome`.
5. [x] Перенести общую навигацию через существующий `gotoWithRetry`, чтобы retry и timeout оставались едиными для всех доменов.
6. [x] Адаптировать `fetchWishlistPagesWithBrowser`, `fetchYandexBooksSearchPageWithBrowser` и `withLitresSearchBrowserSession` так, чтобы они могли работать через уже открытую session/page.
7. [x] Сохранить старые standalone fetcher-функции как совместимые обертки, которые сами создают session и закрывают ее после одного действия.
8. [x] В `scripts/livelib-wish-to-json.mjs` обернуть весь основной workflow в `withBrowserSession`.
9. [x] Передать session-fetcher для LiveLib, Яндекс Книг и Литрес в существующие enrichment-функции вместо standalone browser fetcher.
10. [x] Выполнять подтверждение ручного логина Литрес после открытия Литрес в той же session, не создавая новый context.
11. [x] Обновить browser-тесты: проверить, что основной browser workflow вызывает `launchPersistentContext` один раз за запуск и переиспользует одну страницу.
12. [x] Обновить CLI-тесты моками session API, чтобы зафиксировать порядок: чтение JSON, открытие session, LiveLib, Яндекс Книги, Литрес, запись JSON.
13. [x] Добавить регрессионный тест на закрытие context при ошибке одного из enrichment-этапов.

## Этап 6: длительность аудиокниг

Если среди найденных ссылок на книгу есть аудиокнига, открыть страницу этой
аудиокниги, извлечь длительность и сохранить ее в минутах.

Skill должен уметь:

1. [x] Определять аудиокниги по URL: если ссылка содержит `audiobook`, считать ее кандидатом на извлечение длительности.
2. [x] Открывать страницу аудиокниги в браузерном режиме через существующий постоянный профиль `--profile-dir`.
3. [x] Извлекать длительность аудиокниги из HTML страницы.
4. [x] Нормализовать длительность в количество минут.
5. [x] Записывать длительность в отдельное числовое поле `audiobook_duration_minutes`.
6. [x] Если у книги несколько ссылок на аудиокниги, на первом этапе брать первую подходящую ссылку.
7. [x] Если длительность не найдена, не добавлять поле `audiobook_duration_minutes` и не считать это ошибкой всего запуска.
8. [x] Не запрашивать длительность и не открывать повторно страницы аудиокниг, если `audiobook_duration_minutes` уже записано в данных книги.
9. [x] Делать фиксированную задержку 2000 мс между открытиями страниц аудиокниг.
10. [x] Сообщать статистику: сколько аудиокниг найдено, сколько длительностей извлечено и сколько пропущено из-за уже сохраненных данных.

Технический план:

1. [x] Добавить helper для определения audiobook URL.
2. [x] Добавить parser длительности, который преобразует варианты вроде `8 ч 35 мин`, `8 часов 35 минут`, `35 мин`, `1 ч` в количество минут.
3. [x] Добавить browser fetcher для загрузки страницы аудиокниги.
4. [x] Добавить enrichment-функцию, которая проходит только по книгам с audiobook URL и без уже записанного `audiobook_duration_minutes`.
5. [x] Интегрировать этап после поиска ссылок на Яндекс Книги и Литрес.
6. [x] Добавить тесты на определение audiobook URL, парсинг длительности в минуты, пропуск книг с уже сохраненной длительностью, отсутствие длительности на странице и сохранение результата в JSON.

## Этап 7: описание и изображение книги

Если у книги нет поля `description`, открыть ссылку из поля `url`, найти
описание книги на странице LiveLib и записать его в JSON. Если у книги нет
поля `image`, открыть ссылку из поля `url`, найти изображение книги и записать
абсолютную ссылку на изображение.

Skill должен уметь:

1. [x] После инкрементального merge выбирать только книги, у которых отсутствует или пустое поле `description` или отсутствует или пустое поле `image`.
2. [x] Открывать страницу книги из поля `url` в существующем браузерном workflow через тот же persistent profile `--profile-dir`.
3. [x] Извлекать описание книги из HTML страницы LiveLib без перезаписи уже сохраненного непустого `description`.
4. [x] Нормализовать описание: убрать лишние пробелы, служебный текст и пустые строки, сохранить обычную строку.
5. [x] Извлекать ссылку на изображение обложки книги без перезаписи уже сохраненного непустого `image`.
6. [x] Нормализовать ссылку на изображение в абсолютный URL.
7. [x] Если описание или изображение не найдены, не считать это ошибкой всего запуска и не добавлять пустые поля без необходимости.
8. [x] Не открывать повторно страницу книги, если оба поля `description` и `image` уже заполнены.
9. [x] Делать фиксированную задержку 2000 мс между открытиями страниц книг.
10. [ ] Сообщать статистику: сколько страниц книг открыто, сколько описаний найдено, сколько изображений найдено и сколько книг пропущено из-за уже сохраненных данных.

Технический план:

1. [x] Добавить parser для страницы книги LiveLib, который извлекает `description` и `image` из HTML.
2. [x] Добавить browser fetcher для загрузки страницы книги по `url` через существующую browser session.
3. [x] Добавить enrichment-функцию, которая проходит только по книгам с незаполненным `description` или `image`.
4. [x] Обновлять только отсутствующие поля, сохраняя все уже записанные данные книги, включая ссылки Яндекс Книг, Литрес и длительность аудиокниги.
5. [x] Интегрировать этап после поиска ссылок и длительности аудиокниг, перед записью итогового JSON.
6. [x] Обновить JSON-схему и `SKILL.md`, описав новые необязательные поля `description` и `image`.
7. [x] Добавить тесты на извлечение описания, извлечение изображения, нормализацию относительной ссылки, пропуск уже заполненных полей и сохранение результата в JSON.

## Структура проекта

```text
livelib-wishlist/
├── SKILL.md
├── scripts/
│   ├── livelib-wish-to-json.mjs
│   └── lib/
│       ├── browser.mjs
│       ├── json-output.mjs
│       ├── livelib.mjs
│       ├── text-match.mjs
│       └── yandex-books.mjs
├── .browser-profile/      # локальный профиль браузера, не коммитить
└── tests/
    ├── browser.test.mjs
    ├── cli.test.mjs
    ├── json-output.test.mjs
    ├── livelib.test.mjs
    └── yandex-books.test.mjs
```

## SKILL.md

В `SKILL.md` описать:

- когда использовать skill: если пользователь прислал ссылку на LiveLib wish-list page;
- какой скрипт запускать;
- какой формат входа и выхода ожидается;
- какие ограничения соблюдать.

Пример workflow для агента:

1. Проверить, что ссылка похожа на `/reader/<username>/wish`.
2. Если `--out` уже существует, прочитать текущий JSON до сетевых действий.
3. Запустить скрипт `scripts/livelib-wish-to-json.mjs` в основном браузерном режиме.
4. Открыть публичную страницу wish-list без ожидания ручной авторизации.
5. После открытия wish-list обработать первую страницу и все страницы пагинации текущего wish-list.
6. Объединить существующие книги с книгами из LiveLib: актуальные сохранить, новые добавить, отсутствующие на LiveLib удалить.
7. В browser workflow найти ссылки на Яндекс Книгах и Литрес только для книг без уже заполненных `yandex_books_urls` и `litres_urls`.
8. Сохранить обновленный JSON в тот же файл.
9. Вернуть пользователю количество книг, страниц, новых книг, найденных совпадений Яндекс Книг и Литрес, пропусков и путь к результату.

## CLI-скрипт

Интерфейс:

```bash
node scripts/livelib-wish-to-json.mjs \
  "https://www.livelib.ru/reader/LiraLantan/wish" \
  --out wishlist.json \
  --max-pages 50
```

Рекомендуемые зависимости:

- `playwright` для браузерного режима;
- `cheerio` для парсинга HTML.

Установка браузерной зависимости:

```bash
npm install
npx playwright install chromium
```

Прямой HTTP-загрузчик и режим `--html` можно оставить как fallback, но основной
пользовательский сценарий должен идти через браузерный режим по умолчанию.

## Браузерный режим

Основной режим работы:

1. Запустить Chromium через Playwright в видимом режиме.
2. Использовать постоянный профиль, например `.browser-profile/`, чтобы сессия LiveLib сохранялась между запусками.
3. Открыть URL wish-list, который дал пользователь.
4. Не выполнять отдельную проверку авторизации: публичный wish-list должен открываться без входа.
5. Если LiveLib показывает защитную или промежуточную страницу, не обходить ее автоматически.
6. Не сохранять пароль, cookie или другие данные отдельно от браузерного профиля.

Параметры CLI:

- `--profile-dir .browser-profile` - путь к постоянному профилю браузера;
- `--headed` / видимый режим по умолчанию;
- `--max-pages 50` - лимит страниц пагинации;
- `--max-results 5` - максимум результатов для проверки на одну книгу в каждом поисковом обогащении;
- задержка между запросами фиксированная: 2000 мс для пагинации LiveLib, поиска Яндекс Книг и поиска Литрес.

## Парсинг

Основная логика для каждой страницы:

1. Найти ссылки на книги LiveLib.
2. Оставить ссылки, которые ведут на `/book/...` или `/work/...`.
3. Взять текст ссылки как название книги.
4. Найти автора или авторов рядом с названием.
5. Преобразовать относительные ссылки в абсолютные URL `https://www.livelib.ru/...`.
6. Убрать дубли.
7. Сохранить список объектов в JSON.

## Пагинация

Список книг на обычной странице wish-list может быть разбит на несколько
страниц. Скрипт должен собрать книги со всех страниц текущего списка.

Подход:

1. Начать с URL, который дал пользователь.
2. После открытия страницы в браузере найти ссылки пагинации.
3. Оставить только ссылки того же пользователя и того же раздела `/reader/<username>/wish`.
4. Оставить только реальные ссылки пагинации, а не любые ссылки на `/wish`.
5. Игнорировать футерные, мобильные и трекинговые ссылки, например `utm_*` и `version=mobile`.
6. Нормализовать относительные ссылки в абсолютные URL.
7. Обработать страницы без повторов.
8. Остановиться, когда новых ссылок пагинации больше нет.
9. Использовать лимит безопасности, например `--max-pages 50`.

Скрипт не должен переходить в другие разделы профиля, книги, рецензии,
подборки или страницы других пользователей. Разрешены только URL текущего
wish-list и его пагинации.

Если браузерный режим недоступен, режим `--html` может обработать сохраненный
HTML-файл одной страницы. Это fallback, а не основной сценарий.

## JSON-схема

```json
[
  {
    "title": "string",
    "authors": ["string"],
    "url": "string",
    "yandex_books_urls": ["string"],
    "litres_urls": ["string"],
    "description": "string",
    "image": "string",
    "audiobook_duration_minutes": 123
  }
]
```

Поле `authors` лучше хранить массивом, даже если автор один. Поле
`yandex_books_urls` хранит массив, потому что на Яндекс Книгах может быть
несколько подходящих страниц или изданий. Поле `litres_urls` хранит массив
подходящих страниц Литрес. Поля `description`, `image` и
`audiobook_duration_minutes` необязательные: если данные не найдены, поля можно
не добавлять. Если совпадений на внешних книжных сервисах нет, массив ссылок
должен быть пустым.

## Поиск на Яндекс Книгах

Основная логика обогащения после инкрементального merge:

1. Взять только книги, у которых `yandex_books_urls` отсутствует или пустой.
2. Для каждой такой книги взять `title` и `authors`.
3. Построить поисковый запрос вида `<title> <authors>`.
4. Открыть поиск на `https://books.yandex.ru/`.
5. Извлечь ссылки на страницы книг в домене `books.yandex.ru`.
6. Сравнить текст результата с названием и авторами исходной книги.
7. Добавить все подходящие URL в `yandex_books_urls`.
8. Убрать дубли ссылок.
9. Если совпадений нет, оставить `yandex_books_urls: []`.
10. Если сайт показывает защитную или промежуточную страницу, остановиться без сохранения частичного результата.

Сопоставление должно быть осторожным: лучше не добавить сомнительную ссылку,
чем записать явно неправильную книгу. На первом этапе достаточно простого
эвристического сравнения нормализованных названий и авторов.

Повторный запуск всего skill должен быть инкрементальным: уже сохраненные данные
и ссылки Яндекс Книг переносятся только для книг, которые всё еще есть на
LiveLib, сетевые запросы к Яндексу выполняются только для книг без ссылок, новые
книги добавляются, а исчезнувшие из LiveLib книги удаляются из файла.

## Тесты

Покрыть сценарии:

1. Одна книга.
2. Несколько книг.
3. Несколько авторов у одной книги.
4. Относительная ссылка `/book/...`.
5. Дубли ссылок.
6. Пагинация на две страницы.
7. Дубли страниц пагинации.
8. Ссылка пагинации в другой раздел игнорируется.
9. Футерная ссылка на `/wish` с `utm_*` игнорируется.
10. Ссылка на мобильную версию с `version=mobile` игнорируется.
11. Лимит `--max-pages`.
12. Браузерный режим открывает первую страницу и переходит только по реальной пагинации.
13. Публичная страница wish-list открывается без ожидания ручной авторизации.
14. Пустая страница.
15. Изменившаяся верстка, при которой книги не найдены.
16. Книга найдена на Яндекс Книгах.
17. Книга не найдена на Яндекс Книгах.
18. Несколько подходящих ссылок Яндекс Книг для одной книги.
19. Неподходящие результаты Яндекс Книг игнорируются.
20. Дубли ссылок Яндекс Книг удаляются.
21. Защитная или промежуточная страница Яндекс Книг останавливает обогащение без сохранения частичного результата.
22. Повторный запуск с существующим JSON пропускает книги с уже заполненным `yandex_books_urls`.
23. Повторный запуск ищет только книги, у которых `yandex_books_urls` отсутствует или пустой.

## Ограничения

Для аккуратной работы:

- открывать только страницу, которую дал пользователь, и страницы пагинации этого же wish-list;
- не автоматизировать логин;
- не обходить капчи и защитные механизмы;
- не сканировать другие разделы LiveLib;
- не считать футерные, мобильные и трекинговые ссылки частью пагинации;
- ограничивать количество страниц параметром `--max-pages`;
- ограничивать количество результатов и частоту запросов к Яндекс Книгам;
- хранить сессию только в локальном браузерном профиле `.browser-profile/`;
- не сохранять лишние данные, кроме названия, авторов, ссылки LiveLib и ссылок Яндекс Книг.

## Возможные следующие этапы

После MVP можно добавить:

- экспорт в Markdown или CSV;
- сравнение нового JSON со старым;
- отметку новых и удаленных книг;
- настройку имени выходного файла;
- поддержку нескольких сохраненных HTML-файлов для офлайн-сбора всех страниц;
- прямой HTTP-режим как технический fallback;
- более точное сопоставление книг через ISBN или внешние книжные API.
